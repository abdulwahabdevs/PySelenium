# PySelenium

A beginner-friendly Selenium project that automates browser interactions using Python.

## Features
- Selenium automation with Chrome
- Secure handling of credentials (not committed to GitHub)
- Virtual environment–based setup

## Setup

1. Clone the repository
```bash
git clone https://github.com/YOUR_USERNAME/PySelenium.git
cd PySelenium

2. Install dependencies
pipenv install
pipenv shell

3. Creat a config.py fiel(not comitted) or export credentials to your local terminal
GITHUB_USERNAME = "your_username"
GITHUB_PASSWORD = "your_password"

4. Run the project
python main.py
